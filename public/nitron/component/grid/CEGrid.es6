'use strict';

class CEGrid {
    static row() {
        var element;

        element = {
            name: 'Grid row',
            structure: `<element></element>`
        }

        return element;
    }
}